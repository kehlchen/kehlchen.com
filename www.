<!DOCTYPE html>
<html lang="de">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Braucht man noch www.? Nein!</title>
    <style>
        :root {
            --bg: #0f172a;
            --card-bg: #1e293b;
            --accent: #38bdf8;
            --accent-danger: #f43f5e;
            --text: #f8fafc;
            --text-muted: #94a3b8;
        }

        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
            font-family: system-ui, -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
        }

        body {
            background-color: var(--bg);
            color: var(--text);
            display: flex;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            padding: 20px;
        }

        .container {
            max-width: 650px;
            width: 100%;
            text-align: center;
        }

        .badge {
            display: inline-block;
            background: rgba(244, 63, 94, 0.15);
            color: var(--accent-danger);
            border: 1px solid var(--accent-danger);
            padding: 6px 16px;
            border-radius: 999px;
            font-size: 0.85rem;
            font-weight: 600;
            margin-bottom: 20px;
            text-transform: uppercase;
            letter-spacing: 1px;
        }

        h1 {
            font-size: 2.8rem;
            font-weight: 800;
            line-height: 1.2;
            margin-bottom: 15px;
        }

        h1 span {
            color: var(--accent-danger);
            text-decoration: line-through;
        }

        p.subtitle {
            font-size: 1.2rem;
            color: var(--text-muted);
            margin-bottom: 35px;
        }

        .card {
            background-color: var(--card-bg);
            border: 1px solid #334155;
            border-radius: 16px;
            padding: 30px;
            box-shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.3);
            margin-bottom: 30px;
            text-align: left;
        }

        .tool-title {
            font-size: 1rem;
            font-weight: 600;
            margin-bottom: 12px;
            color: var(--text-muted);
        }

        .input-group {
            display: flex;
            gap: 10px;
            margin-bottom: 20px;
        }

        input {
            flex: 1;
            background: #0f172a;
            border: 1px solid #334155;
            border-radius: 8px;
            padding: 12px 16px;
            color: var(--text);
            font-size: 1rem;
            outline: none;
            transition: border-color 0.2s;
        }

        input:focus {
            border-color: var(--accent);
        }

        .result-box {
            background: #0f172a;
            border-radius: 8px;
            padding: 15px;
            font-family: monospace;
            font-size: 1.1rem;
            display: flex;
            align-items: center;
            justify-content: space-between;
        }

        .result-text {
            color: var(--accent);
            word-break: break-all;
        }

        .features {
            display: grid;
            gap: 15px;
            text-align: left;
        }

        .feature-item {
            display: flex;
            align-items: flex-start;
            gap: 12px;
        }

        .feature-icon {
            font-size: 1.2rem;
        }

        footer {
            color: var(--text-muted);
            font-size: 0.85rem;
            margin-top: 20px;
        }
    </style>
</head>
<body>

    <div class="container">
        <div class="badge">Eilmeldung aus dem Jahr 2004</div>
        
        <h1>Lass das <span>www.</span> einfach weg!</h1>
        <p class="subtitle">Wir schreiben nicht mehr das Jahr 1998. Dein Browser weiß bereits, dass du im Internet bist.</p>

        <div class="card">
            <div class="tool-title">⚡ Mach deine URL 4 Zeichen kürzer:</div>
            <div class="input-group">
                <input type="text" id="urlInput" placeholder="www.deine-website.de" value="www.google.de">
            </div>
            <div class="result-box">
                <span>So reicht es vollkommen:</span>
                <strong id="resultText" class="result-text">google.de</strong>
            </div>
        </div>

        <div class="card features">
            <div class="feature-item">
                <span class="feature-icon">🚀</span>
                <div>
                    <strong>Du sparst wertvolle Lebenszeit:</strong>
                    <p style="color: var(--text-muted); font-size: 0.9rem;">4 Tastenanschläge gespart. Bei 10 Websites am Tag sind das... immerhin ein paar Sekunden pro Jahr!</p>
                </div>
            </div>
            <div class="feature-item">
                <span class="feature-icon">✨</span>
                <div>
                    <strong>Es sieht besser aus:</strong>
                    <p style="color: var(--text-muted); font-size: 0.9rem;">Visitenkarten, Plakate und Links wirken ohne das Prefix doppelt so clean.</p>
                </div>
            </div>
            <div class="feature-item">
                <span class="feature-icon">🧠</span>
                <div>
                    <strong>Moderne Server regeln das:</strong>
                    <p style="color: var(--text-muted); font-size: 0.9rem;">Nahezu jede Domain leitet heutzutage automatisch richtig weiter (DNS Apex Redirection).</p>
                </div>
            </div>
        </div>

        <footer>
            Schicke diese Seite an jemanden, der immer noch "www." tippt.
        </footer>
    </div>

    <script>
        const urlInput = document.getElementById('urlInput');
        const resultText = document.getElementById('resultText');

        urlInput.addEventListener('input', () => {
            let value = urlInput.value.trim();
            
            // Entferne Protokolle und www.
            value = value.replace(/^(https?:\/\/)?(www\.)?/, '');
            
            if (value.length === 0) {
                resultText.textContent = 'deine-seite.de';
            } else {
                resultText.textContent = value;
            }
        });
    </script>
</body>
</html>
